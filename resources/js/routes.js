import Vue from 'vue';
import VueRouter from 'vue-router';

Vue.use(VueRouter);

function joinRoutes() {

    let routes = [];

    return routes.concat(

    );
}

const router = new VueRouter({
    routes: joinRoutes(),
    mode: 'history'
});

export default router;
